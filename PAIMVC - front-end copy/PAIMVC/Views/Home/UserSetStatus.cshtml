@model PAIMVC.Models.Requests.UserRegisterRequest
@{
    ViewData["Title"] = "UserStatusSet";
}

<h1><center>USER STATUS SET</center></h1>
<br />
<div class="page-content container-fluid">
    <div class="" data-plugin="matchHeight" data-by-row="true">
        @using (Html.BeginForm("PUserSetNegativeStatus", "Home", FormMethod.Post))
        {
            <div class="panel">
                <div class="panel-body">
                    <div class="">
                        <div class="form-group row">
                            <div class="col-sm-12">
                                <div class="form-group row">
                                    @Html.LabelFor(model => model.Hash, new { @class = "col-sm-3 col-form-label" })
                                    <div class="col-sm-9">
                                        @Html.TextBoxFor(model => model.Hash, new { @class = "form-control" })
                                        @Html.ValidationMessageFor(model => model.Hash)
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="rowError">@Html.ValidationSummary(true)</div>

                        <div class="text-right">
                            <a href="@Url.Action("Index", "Home")" class="button" style="margin-right: 10px;">Powrót</a>
                            <button type="submit" value="True" class="btn btn-primary" id="validateButton2">Zapisz zmiany</button>
                        </div>
                    </div>
                </div>
            </div>
        }

    </div>
</div>
